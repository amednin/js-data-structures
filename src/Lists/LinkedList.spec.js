// import { expect } from 'chai';
import { LinkedList } from './index';

describe('LinkedList class', () => {
  it('should insert a new value to first node', () => {
    const list = new LinkedList();
    // list.insert(1);
    // expect(list.length()).to.eql(1);
  });
});
